<!DOCTYPE html>
<html>
  <head>
    <title>TmpProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %> <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <p class="navbar-text float-right">
      <% if technician_signed_in? %>
        Logged in as <strong><%= current_technician.email %></strong>.
        <%= link_to 'Edit profile', edit_technician_registration_path, :class => 'navbar-link' %> |
        <%= link_to "Logout", destroy_technician_session_path, method: :delete, :class => 'navbar-link'  %>
      <% else %>
        <%= link_to "Sign up", new_technician_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Login", new_technician_session_path, :class => 'navbar-link'  %>
      <% end %>
    </p>
    <%= yield %>
  </body>
</html>
